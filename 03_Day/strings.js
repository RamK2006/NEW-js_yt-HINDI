const name = "Ram-Bhakt"
const repoCount = 16
console.log(name + repoCount + "Value");

console.log(`Hello my name is ${name} with repo count ${repoCount}`);
name.__proto__
name.toUpperCase()
console.log(name)